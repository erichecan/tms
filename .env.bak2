# TMS SaaS平台环境配置示例
# 复制此文件为 .env 并修改相应配置

# 应用配置
NODE_ENV=development
PORT=8000
API_VERSION=v1

# 数据库配置
# ==========================================
# 注意：所有数据库连接统一使用 DATABASE_URL 环境变量
# 不再使用独立的 DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD 变量
# 如果 DATABASE_URL 未设置，应用将无法启动
# ==========================================
DATABASE_URL=postgresql://neondb_owner:npg_lZq2bWeJT8tO@ep-round-math-ahvyvkcx-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Redis配置
REDIS_URL=redis://localhost:6379
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# 文件上传配置
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# 邮件配置
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@tms-platform.com

# 短信配置
SMS_PROVIDER=twilio
SMS_ACCOUNT_SID=your-twilio-account-sid
SMS_AUTH_TOKEN=your-twilio-auth-token
SMS_FROM_NUMBER=+1234567890

# 日志配置
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# 监控配置
ENABLE_METRICS=true
METRICS_PORT=9090

# 安全配置
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 第三方集成
WEATHER_API_KEY=your-weather-api-key

# 开发配置
ENABLE_SWAGGER=true
ENABLE_DEBUG=true

# ============================================================================
# Google Maps API Key 配置 - 2025-01-27 17:30:00 更新
# ============================================================================
# 后端 API Key: 用于服务器端地址解析（Geocoding）、路线规划（Directions）、距离矩阵（Distance Matrix）
# 安全限制：建议设置 IP 地址限制或 API 限制（只允许特定 API）

# 前端 API Key: 用于客户端地图显示（Maps JavaScript API）、地址自动完成（Places API）
# 变量名必须以 VITE_ 开头，Vite 才会在构建时注入
# 安全限制：必须设置 HTTP 引用来源限制（域名白名单）
VITE_GOOGLE_MAPS_API_KEY=AIzaSyD26kTVaKAlJu3Rc6_bqP9VjLh-HEDmBRs

# 地图显示 API Key（可选，如果与前端 API Key 不同）
# 如果使用不同的 Key 用于地图显示，可以取消下面的注释
# VITE_GOOGLE_MAPS_DISPLAY_API_KEY=AIzaSyBAdoCwFgCy6zvuVnHgu0lWptF1eAnyApY

# Google Maps 控制配置 - 2025-01-27 16:55:00
GM_DEBOUNCE_MS=400
GM_CACHE_TTL_MS=60000
GM_MAX_CALLS_PER_SESSION=200
