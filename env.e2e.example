# E2E 测试环境变量配置示例 - 生产环境
# 创建时间: 2025-12-10 20:00:00
# 使用方法: 复制此文件为 .env.e2e 并填入实际值

# ============================================================================
# 生产环境配置
# ============================================================================

# 生产环境基础 URL（必须）
# 示例: https://tms.example.com
PROD_BASE_URL=https://your-production-domain.com

# 生产环境测试账号（必须）
# 注意：此账号需要有创建订单和查看地图的权限
PROD_TEST_USER=admin@example.com
PROD_TEST_PASSWORD=your-secure-password

# 规则引擎 URL（可选，用于精确匹配规则引擎调用）
# 如果未设置，测试将使用通用路径匹配
# 示例: https://api.example.com/api/rules/evaluate
RULE_ENGINE_URL=

# Google Maps 期望的端点列表（可选，逗号分隔）
# 可选值: js, geocoding, directions, distance-matrix, static, places
# 默认: js,geocoding
GOOGLE_MAPS_EXPECTED_ENDPOINTS=js,geocoding

# ============================================================================
# 本地开发环境配置（可选）
# ============================================================================

# 本地开发环境基础 URL（用于本地测试）
BASE_URL=http://localhost:3000

# 本地测试账号
E2E_USERNAME=admin@example.com
E2E_PASSWORD=admin123

# ============================================================================
# Playwright 配置（可选）
# ============================================================================

# 是否在 headed 模式下运行（显示浏览器窗口）
# HEADED=false

# 浏览器类型（chromium, firefox, webkit）
# BROWSER=chromium

# 测试超时时间（毫秒）
# TEST_TIMEOUT=120000

# ============================================================================
# 重要提示
# ============================================================================
# 1. 生产环境测试需要谨慎使用，建议：
#    - 使用专门的测试账号
#    - 避免在生产环境创建真实订单（如果可能，使用报价预览模式）
#    - 定期清理测试数据
#
# 2. 如果生产策略不允许创建真实订单，请：
#    - 将保存动作替换为报价预览
#    - 或使用 staging 环境进行测试
#
# 3. 敏感信息（如密码）不应提交到版本控制系统
#    - 使用 .env.e2e（已添加到 .gitignore）
#    - 在 CI/CD 中使用环境变量密钥
